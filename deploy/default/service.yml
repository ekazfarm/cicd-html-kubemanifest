apiVersion: v1
kind: Service
metadata:
  name: svc-htmleka
  namespace: default
  labels:
    app: htmleka
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: htmleka
